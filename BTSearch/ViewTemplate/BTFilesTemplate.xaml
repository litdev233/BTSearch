<UserControl
    x:Class="BTSearch.BTFilesTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BTSearch"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
        <Image Source="{x:Bind BTFile.file_icon}" HorizontalAlignment="Left" Width="10" Height="10" VerticalAlignment="Center"/>
        <TextBlock Name="MovieTitle" FontSize="12" Margin="5,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{x:Bind BTFile.file_title}" />
        <TextBlock FontSize="12" Margin="5,0,0,0" Text="{x:Bind BTFile.file_size}" VerticalAlignment="Center" />

    </StackPanel>
</UserControl>
